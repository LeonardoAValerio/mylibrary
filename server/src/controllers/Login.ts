import { Router, Request, Response } from 'express';

const loginController = Router();

loginController.post("/", ((req: Request, res: Response) => {
    
}));

loginController.post("/create-account", ((req: Request, res: Response) => {
    
}));